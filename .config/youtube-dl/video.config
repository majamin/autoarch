# Ignore errors (skip to next item on error)
--ignore-errors

# Download the playlist, if URL refers to video and playlist
--yes-playlist

# Nice filenames
--restrict-filenames

# Prefer mkv
--merge-output-format mkv

# Thumbails
--embed-thumbnails

# Add extra data
--add-metadata

# Save all videos under Movies directory in your home directory
-o /mnt/BarraCuda/Videos/playlist_uploader/playlist_title/%(title)s_%(id)s.%(ext)s
