#!/bin/sh

AFILES=$(ls | file -if- | grep audio | awk -F: '{print $1}' | sed 's/\(.*\)/\x27\1\x27/g')

#echo $AFILES

for i in $AFILES;
do
	name=$(echo "$i" | cut -d'.' -f1)
	echo "$name"
	#ffmpeg -i "$i" "${name}.flac" && rm "$i"
done
